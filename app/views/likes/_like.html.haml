- if current_user
  .Post-empathy
    %p.Post-empathy__text 共感
    - if current_user.already_liked?(post)
      = link_to post_like_path(post, current_user.likes.find_by(post_id: @post.id)), method: :delete, remote: true, class: "Post-empathy__link, Post-empathy__link--red" do
        = icon('fas', 'heart', class: 'Post-empathy__icon')
    - else
      = link_to post_likes_path(post), method: :post, remote: true, class: "Post-empathy__link, Post-empathy__link--gray" do
        = icon('fas', 'heart', class: 'Post-empathy__icon')
    %p.Post-empathy__number
      = post.likes.count